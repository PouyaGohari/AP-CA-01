#include <iostream>
#include <vector>
#include <fstream>
using namespace std;
struct Input_shape{
    int time_slot_counts;
    vector<string> participants;
    vector<vector<string>> votes;
};

int main(){
    return 0;
}